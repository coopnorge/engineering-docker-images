# https://github.com/safe-waters/docker-lock/blob/master/.docker-lock.example.yml
generate:
  base-dir: .
  dockerfile-globs:
    - "Dockerfile"
    - "Dockerfile.*"
    - "*.Dockerfile"
  kubernetesfile-recursive: true
  dockerfile-recursive: true
  update-existing-digests: true
