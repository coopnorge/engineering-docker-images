services:
  devtools:
    image: ocreg.invalid/coopnorge/engineering/image/devtools-terraform:built
    working_dir: /srv/workspace
    volumes:
      - .:/srv/workspace:z
      - xdg-cache-home:/root/.cache
volumes:
    xdg-cache-home: {}
