services:
  devtools:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/srv/workspace:z
      - ${XDG_CACHE_HOME:-xdg-cache-home}:/root/.cache
    working_dir: /srv/workspace

volumes:
  xdg-cache-home: { }
