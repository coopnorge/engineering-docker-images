services:
  devtools:
    image: ${IMAGE_UNDER_TEST:-ocreg.invalid/coopnorge/engineering/image/devtools-golang-v1beta1:built}
    working_dir: /srv/workspace
    volumes:
      - .:/srv/workspace:z
      - xdg-cache-home:/root/.cache
volumes:
    xdg-cache-home: {}
